server:
  port: 8088
spring:
  application:
    name: pharmacy-app
  datasource:
    url: jdbc:mysql://localhost:3306/pharmacy-app?createDatabaseIfNotExist=true
    username: root
    password: Adem147852369..
  jpa:
    show-sql: true
  jackson:
    time-zone: GMT+1
    date-format: yyyy-MM-dd HH:mm:ss
    serialization:
      write-dates-as-timestamps: false
  flyway:
    baseline-on-migrate: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  jwt:
    secret: ${JWT_SECRET:PharmaSeekAppSecretKey2026VeryLongKey}
    accessTokenValidity: 7200000
    refreshTokenValidity: 604800000
    secureCookie: false
    sameSite: Lax
